create table trained_in
(
    physician INTEGER
    treatment INTEGER
    certificationdaate DATE
    certificationexpires DATE
)
select * from trained_in
insert into trained_in(physician,treatment,certificationdate,certificationexpires)
values();
